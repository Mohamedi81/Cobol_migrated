version: "3.8"
services:
  db:
    image: postgres:16
    container_name: genapp-db
    environment:
      POSTGRES_DB: genapp
      POSTGRES_USER: genapp
      POSTGRES_PASSWORD: genapp
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
